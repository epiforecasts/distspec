% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dist_spec.R
\name{resample_bounded_rows}
\alias{resample_bounded_rows}
\title{Resample rows of a composite sample matrix that violate a row-sum bound}
\usage{
resample_bounded_rows(x, samples, max_value, max_attempts = 100)
}
\arguments{
\item{x}{A \verb{<multi_dist_spec>}.}

\item{samples}{The \code{n} by \code{k} matrix of per-component samples to fix up.}

\item{max_value}{The upper bound on \code{rowSums(samples)}.}

\item{max_attempts}{The maximum number of resampling rounds.}
}
\description{
Used by \code{sample_dist.multi_dist_spec()} to enforce a \code{max}/\code{cdf_max} set on
the composite itself. There is no closed-form CDF for an arbitrary sum of
components, so this redraws every component of each violating row until its
sum satisfies the bound, capped at \code{max_attempts} rounds so a bound that is
effectively unreachable errors instead of hanging.
}
\keyword{internal}
