% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dist_spec.R
\name{sample_bounded}
\alias{sample_bounded}
\title{Draw samples respecting a \code{max}/\code{cdf_max} bound}
\usage{
sample_bounded(x, n, rng, cdf, quantile)
}
\arguments{
\item{x}{A single (non-composite) \verb{<dist_spec>}.}

\item{n}{The number of samples to draw.}

\item{rng}{The base-R random-generation function for the family (e.g.
\code{\link[=rgamma]{rgamma()}}).}

\item{cdf}{The base-R CDF function for the family (e.g. \code{\link[=pgamma]{pgamma()}}).}

\item{quantile}{The base-R quantile function for the family (e.g.
\code{\link[=qgamma]{qgamma()}}).}
}
\value{
A numeric vector of \code{n} samples.
}
\description{
Used by each parametric distribution's \code{sample_dist()} method. If \code{x} is
unconstrained this just calls the unbounded generator \code{rng}. Otherwise it
draws exactly from the bounded distribution via inverse-CDF sampling:
\code{upper} is the smaller of \code{max} and the \code{cdf_max} quantile, \code{u} is drawn
uniformly on \verb{(0, F(upper))}, and the quantile at \code{u} is returned. This
takes a single pass, unlike a rejection loop (resampling from the unbounded
distribution until a draw falls within the bound), which can hang when the
bound cuts off nearly all of the mass (e.g. \code{Normal(mean = 100, sd = 1,
max = 90)}, whose tail beyond 90 has probability of order 1e-24).
}
\keyword{internal}
